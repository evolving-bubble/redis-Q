'use strict';

// Internal
const Libs = require('../lib');

class Publisher {
    constructor(options) {
        let self = this;

        let redisPublisherOptions = {

        };

        self.redis = new Libs.redis({

        });
    }


}